[package]
name = "hippo-api"
version = "0.1.0"
edition = "2021"
description = "Shared HTTP request/response types for the Hippo knowledge graph"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
